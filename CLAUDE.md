# CLAUDE.md — larabill

Entry point para Claude Code en este paquete. Hereda del paraguas `~/development/packages/aichadigital/CLAUDE.md` (no duplicar reglas de bot de dependencias, webhooks Packagist, branch protection, lecciones aprendidas — están allí).

## Qué es este paquete

Larabill es el **núcleo de facturación** del ecosistema Larafactu (AichaDigital):

- Facturas inmutables con UUID v7 (string char 36, no binario — ADR-006 consolida el estándar)
- Cálculo fiscal (España, UE, mundial); la verificación VAT/NIF intracomunitaria la posee `lararoi` — larabill la consume vía un puente fino opcional (`Actions\VerifyVatNumber`, delega en `VatVerificationServiceInterface`), NO acoplado a la emisión (reverse-charge decide por `is_roi_taxed`) (AID-309)
- Datos fiscales temporales (emisor `CompanyFiscalConfig` + receptor `UserTaxProfile`)
- Pricing por frecuencia (`Article` + `ArticlePrice`, ADR-004)
- Cumplimiento VeriFACTU (vía `lara-verifactu`)

**Stack:** PHP 8.3+, Laravel 12 ó 13 (`^12.0||^13.0`), Pest. Framework-agnóstico (sin acoplamiento a Filament). License AGPL-3.0-or-later.

## Lectura obligatoria antes de tocar código

1. **`.claude/CRITICAL_RULES.md`** — inyectado por SessionStart hook, son las reglas duras
2. **`.claude/project.md`** — contexto exhaustivo de arquitectura, modelos, ADRs
3. **`SCHEMA_REQUIREMENTS.md`** — qué exige al `users` de la app consumidora
4. **`CONTRIBUTING.md`** — patrón de migraciones (.php + .stub + `$migrationOrder`)
5. **`docs/ADR-*.md`** — decisiones arquitectónicas vigentes (ficheros reales): 003 (unificación user/customer), 004 (precios por frecuencia), 006 (UUID-first), 007 (`.php.stub` derivado del `.php`), 008 (puerto de retención legal — `Invoice`/`UserTaxProfile` implementan `LegallyRetainable`; larabill NO ejecuta privacidad operativa), 009 (tipo de dinero `FixedDecimal`), 010 (mantener el publishing de migraciones; rechazar «0 stubs» — cierra AID-302), 011 (frontera del PDF — vocabulario canónico de plantillas, validación post-render del contenido fiscal, `Invoice::generatePDF()` como `@api`; cierra AID-502). Las decisiones previas 001/002/005 quedaron consolidadas en estas.
6. **`docs/2026-05-09-fresh-install-agnostic-mysql.md`** — histórico: describía un contrato agnóstico `int`/`uuid`/`ulid` **superado por ADR-006**. El contrato vigente es **UUID-first** (ver ADR-006), demostrado en `tests/Integration/Mysql/`.
7. **`docs/2026-05-09-blocker-upgrade-test-customer-id-bigint-to-uuid.md`** — SUPERSEDED, conservado como rastro histórico de una premisa rota (no había producción, dev-main no promete upgrade).

## Reglas inviolables (resumen — detalle en CRITICAL_RULES.md)

- **Migraciones:** toda tabla del paquete tiene `.php` (timestamped, auto-load en dev) **Y** `.php.stub` (publicado por `larabill:install`). Solo 2 stubs son consumer-only (modifican el `users` del consumidor): `add_user_relationships_to_users_table.php.stub` y `rename_user_id_to_owner_user_id_in_user_tax_profiles.php.stub`.
- **El `.php.stub` es artefacto derivado del `.php` (ADR-007):** byte-idéntico, regenerado con `bin/sync-migration-stubs`. NUNCA editar un `.stub` a mano — editar el `.php`, correr el script, commitear ambos.
- **`$migrationOrder`** en `LarabillInstallCommand` debe coincidir 1:1 con los stubs reales (32 entradas hoy). Un test de consistencia (`tests/Unit/Console/MigrationOrderConsistencyTest.php`) valida 1:1 + byte-identidad `.php`↔`.stub` en CI.
- **FK a users:** SIEMPRE `MigrationHelper::userIdColumn($table, 'col')`. Nunca `$table->foreignId()` directo. Emite UUID v7 char(36) exclusivamente (ADR-006); `int`/`ulid`/`larabill.user_id_type` retirados en v0.8.0.
- **Dinero:** SIEMPRE Base-100 entero (`12,34 € → 1234`) con cast `FixedDecimalCast:2` de `lara100` (value object `FixedDecimal`, AID-237). NUNCA float/decimal. La columna sigue `integer`; el cast materializa el value object.
- **Numeración fiscal: SIEMPRE `InvoiceNumberingService::generateNumber()`** (dueño único, AID-390). `fiscal_number`, `prefix`, `series_number` y `fiscal_year` salen del MISMO `InvoiceNumber`. El scope del contador es el EMISOR (null → sentinel `GLOBAL_SCOPE`), nunca el cliente facturado (`billable_user_id`, ADR-003). La **serie** (`prefix`) que se le pasa se resuelve SIEMPRE con `InvoiceSeriesResolver` (AID-307), nunca hardcode `'FAC'`/`'PRO'`; el `serie` (int) es el TIPO fiscal, distinto de la serie.
- **Precios de artículo: a lo sumo UNO activo por `(artículo, frecuencia)` en cualquier fecha** (AID-601, ADR-012). La condición de solape tiene fuente única (`ArticlePrice::scopeOverlapping()`); el hook `saving` es la red y **`ArticlePriceService::setPrice()` es la garantía** — usarlo siempre que importe la concurrencia (bloquea la fila padre de `articles`, no los precios: `FOR UPDATE` no serializa un alta que casa con cero filas). Datos preexistentes: `larabill:diagnose-price-overlaps`.
- **Facturas emitidas son inmutables.** No editar nada con status ≠ `draft`.
- **Tests agnósticos:** usar `config('larabill.user_model')` para aserciones de tipo, no hardcodear `User::class`.

## Comandos

Ejecutar dentro de `larabill/`:

```bash
composer prepare              # testbench package:discover (post-install/update)
composer test                 # pest
composer test-parallel        # pest --parallel
composer test-coverage        # pest --coverage
composer pint                 # formato
composer phpstan              # análisis estático (memory-limit=1G)
composer phpinsights          # insights
composer quality              # pint + phpstan + test-coverage
composer precommit            # pint + phpstan + test-parallel
```

Filtros típicos: `vendor/bin/pest --filter='nombre del test'`, `vendor/bin/pest tests/Unit/Foo/BarTest.php`.

## Nota sobre PHP 8.4 local

Hay un bug conocido "table already exists" con SQLite in-memory en PHP 8.4 local. Usar PHP 8.3 localmente o confiar en CI (matriz PHP 8.3+8.4 × L12+L13).

## Anti-patterns frecuentes

- Crear `.php.stub` sin su `.php` → no se auto-carga en tests/dev
- Añadir entrada a `$migrationOrder` sin el stub correspondiente → install falla
- Usar `hasMigration()` de Spatie → exige `vendor:publish` manual, rompe el flujo del install command
- Asumir `Auth::user()->id` como int → larabill es UUID-first (ADR-006); romper esa asunción en helpers, DTOs, casts o tests
- Reintroducir `larabill.user_id_type`, `LARABILL_USER_ID_TYPE`, soporte `int`/`ulid`, o el flag CLI `--user-id-type=` → eliminados en v0.8.0 (ADR-006). Solo se reabre con caso de cliente concreto (ver ADR-006 §"Criterios de reapertura").
- Reintroducir `Customer`/`CustomerFiscalData` → deprecados desde ADR-003 (usar `User` con `parent_user_id` + `UserTaxProfile`)
- Reintroducir UUID binario → descartado; el estándar es UUID v7 char(36) (ADR-006). Incompatibilidad histórica con Filament, dependencia ya eliminada del paquete.
- Una migración que transforma datos SIN test de su camino de upgrade → desde AID-398 la promesa de upgrade SÍ se asume (versión estable = respeto por datos): sembrar el estado legacy → correr la migración → verificar (patrón: test del backfill AID-390). La regla histórica «dev-main no promete upgrades» quedó obsoleta con la 1.0.
- Reintroducir un generador de numeración propio en un servicio (`rand()`, contador en cache, MAX+1) → la numeración fiscal tiene dueño único `InvoiceNumberingService` (AID-390); delegar siempre
- Confundir el TIPO fiscal (`serie`/`InvoiceSerieType`) con la SERIE real (`prefix`) → desde AID-307 (v5.0.0) son cosas distintas: el tipo va a AEAT como `TipoFactura`, la serie (`prefix`) como `NumSerieFactura`. Nunca hardcodear `'FAC'`/`'PRO'` (usar `InvoiceSeriesResolver`), ni mandar `$invoice->serie->value` como serie a Verifactu, ni volver al unique de `invoices` sin `prefix`.
- Sembrar `invoice_series_control` con `user_id => null` en fixtures → el scope global es el sentinel `InvoiceSeriesControl::GLOBAL_SCOPE`; NULL quedó retirado (no colisiona en unique)
- Asumir que `composer test` (suite SQLite) demuestra el contrato real → SQLite no preserva índices compuestos ni longitudes char(36) reales. El contrato se demuestra en `tests/Integration/Mysql/` contra MySQL 8.
- Hardcodear `'user_id' => 1` (o cualquier int) en fixtures de test → usar las constantes `TestCase::USER_UUID_1/2/3`.
- Reintroducir superficie retirada en v6.0.0 (AID-423) → `BillingService` (usar `InvoiceService`), los shims `user_id` de `UserTaxProfile` (`user()`, `user_id`, `getActiveForUser`, `getValidForUserAt`, `createForUser`, scope `forUser` → superficie `owner*`), los aliases de `HasUserRelationships` (`taxProfiles`, `activeTaxProfile`, `withActiveTaxProfile`). En fixtures de `UserTaxProfile` la columna es `owner_user_id` (las factories son unguarded: un `'user_id'` va directo a una columna inexistente).
- Proponer un breaking sin imperativo de uso cualificado y documentado → contradice `STABILITY.md` (vigente desde v6.0.0); los majors no se gastan en gusto estético.
- Consultar `invoice_templates`/`CompanyTemplateSettings` con el label de la serie (`getInvoiceType()`) o hardcodear tipos de plantilla → desde v6.6.0 (ADR-011) el vocabulario del registro es `TemplateInvoiceType::registryKey()` y la derivación única es `DomPDFService::resolveTemplateType()`; el label de serie solo vive en `pdfFilename()`.
- Desactivar `larabill.pdf.validate_fiscal_content` para «arreglar» un test rojo de render → el rojo significa que la plantilla tira contenido fiscal obligatorio (ADR-011); se corrige la plantilla, nunca el guard. El escape de config es para instalaciones que asumen el riesgo conscientemente, no para la suite.
- Sembrar dos precios ACTIVOS solapados de la misma `(artículo, frecuencia)` → desde AID-601 el paquete lo rechaza (`OverlappingArticlePriceException`); en fixtures, dar rangos disjuntos, frecuencias distintas o artículos distintos. Y escribir precios saltándose `ArticlePriceService` donde haya concurrencia real: el hook `saving` es red de seguridad, no garantía (medido: sin el lock, 5-6 de 6 procesos concurrentes escriben).
- Corregir el comentario del índice de `create_article_prices_table` para que deje de prometer lo que no da → la migración está publicada y en el manifiesto con su `sha256` (D6 de ADR-012). `MigrationOrderConsistencyTest` da **falso verde** ante ese cambio (compara `.php` con `.php.stub`); el que se pone rojo es `ShippedMigrationImmutabilityTest`.
- Dar un default ALEATORIO (`faker->boolean(N)`, `optional()`, `randomElement`) a un campo de factory que gobierna un guard, policy, visibilidad o resolución (`is_immutable`, `is_roi_taxed`, `is_exempt_vat`, flags de estado) → es un flake latente (AID-508/AID-558/AID-581): el día que aparece el guard, ~N% de los fixtures que no lo declaran flakean. El default es determinista (`false`/valor típico); el test que necesita el caso lo declara. Ante un flake por moneda de factory, la corrección es DE-RANDOMIZAR el default, no pinnear el fixture afectado (deja el resto de monedas vivas).

## Estado actual (2026-07-19)

- Tag actual: **v6.7.0** (Packagist); `main` limpio; `[Unreleased]` vacío. **PHPStan level 8** (máximo). Cadena reciente:
  - **v6.7.0** (2026-07-19) — **AID-576: serie fiscal per-call en la conversión** (cero migraciones, MINOR): `InvoiceService::convertProformaToInvoice()` acepta `'series' => ?string` en `$options` y lo reenvía a `$invoiceData['series']` (que `createInvoice()` ya resuelve vía `InvoiceSeriesResolver`). `make_immutable` NO se expone: la inmutabilidad canónica sigue obligatoria (una conversión mutable dejaría un estado inválido que la propia idempotencia devolvería como conversión completada). Defaults intactos — sin opción o `null` → resolver default, numeración exactamente como antes. Adición `@api` sobre método `@api` (semver-minor). Es el handoff §3 de la reconciliación AID-576 (la ruta manual de conversión Vía 1 de Castris delega en la canónica); el lado consumer (puerto `ProformaConverter` + adapter, `registerConvertedInvoice`) vive en el repo `clientes`. Colateral cerrado en el mismo día: **AID-581** — `InvoiceFactory.is_roi_taxed` era una moneda `faker->boolean(10)` (la «otra moneda viva» que dejó AID-558); de-randomizada a `false` (misma clase AID-508/AID-558) + regression pin, mató un flake de `FiscalContentGuardWiringTest` bajo `--parallel` (~10%: la moneda enrutaba fixtures fiscales a REVERSE_CHARGE → blade conforme → guard no dispara).
  - **v6.6.0** (2026-07-18) — **AID-502 → ADR-011, la frontera del PDF** (cero migraciones, MINOR): (D1) vocabulario canónico de plantillas — `TemplateInvoiceType::registryKey()` es el literal de `invoice_templates.type` y `DomPDFService::resolveTemplateType()` la derivación única serie→tipo; el registro resuelve POR PRIMERA VEZ para facturas fiscales (antes consultaba con el label de serie `'invoice'` contra filas `type='fiscal'`), la fila default gobierna también la vista, y las lookups de notes/settings de reverse-charge/exempt alcanzan su propio brazo. (D2a) `FiscalContentValidator` post-render: todo dato fiscal obligatorio no vacío entregado a la plantilla debe aparecer en el HTML (RD 1619/2012 arts. 6/7; simplificadas exentas de identificación del receptor; fidelidad de render, no completitud de emisión); config `larabill.pdf.validate_fiscal_content` default **true**; contrato público = `FiscalContentMissingException`. (D3) `Invoice::generatePDF()` promovido a `@api` a nivel de método (banda amber 7→8) con el shape del resultado como contrato. (D4) no-action sobre connectors, registrado con criterios de reapertura (spatie/laravel-pdf aplazado).
  - **v6.5.1** (2026-07-18) — 4 fixes sin migraciones: AID-503 (branch-alias `dev-main` retirado del composer.json), AID-534 (`fiscal_snapshot.tax_rules_applied` se puebla agregando los `taxes_applied` congelados de las líneas — ya no persiste `[]`), AID-537 (QR fiscal normalizado a 35mm — lara-verifactu emite 300px≈79mm, el doble de la banda AEAT 30-40mm — + rechazo de `href`/`url()` externos en el SVG), AID-535 (la frontera `PDFService` captura `\Throwable` y es el ÚNICO logger del subsistema).
  - **v6.5.0** (2026-07-18) — **hardening quirúrgico de la conversión proforma→factura (D1-D10 del forense v7)**: AID-553 (cadena `models.user`→`user_model`→RuntimeException loud; el fallback a la clase de tests murió), AID-554 (D1 `lockForUpdate` en la idempotencia de conversión), AID-555 (D2 `proforma_id` canónico + migración data `backfill_invoice_proforma_link` — la ÚNICA migración de la tanda), AID-556+557 (D3+D4 clonado verbatim de línea: importes congelados de la proforma, jamás recalculados contra el catálogo vivo), AID-558 (D8 guard de inmutabilidad en `InvoiceItem::update()`), AID-559 (D10 `service_date` viaja verbatim en conversión y creación — jamás se inventa una fecha), AID-570 (retry anti-deadlock en las transacciones EXTERIORES — el `attempts` interior es savepoint y no salva nada). Además AID-536: **mutation testing NATIVO** `pest --mutate` (Infection no tiene adapter Pest), scope `mutates()` en las suites de money+fiscal, gate CI no-required `--min=68` (baseline medido 68.81%).
  - **v6.4.1** (2026-07-18) — AID-546: el PDF imprime el receptor congelado del `customer_snapshot` (ADR-001), no el perfil vivo; la exención VAT también se congela.
  - **v6.4.0** (2026-07-17) — **AID-508: el PDF vuelve a ser una factura** (número real, líneas reales, totales de columnas reales, emisor del snapshot congelado, QR solo del registro fiscal — jamás fabricado, desglose por tipo real, el servicio PUEDE fallar). Sin migraciones.
  - **v6.3.1** (2026-07-13) — **AID-328** (cero migraciones): `FiscalChangeDetector` deja de ser ciego a ediciones in-place de las fiscal configs. El lado congelado de la comparación pasa a ser el **snapshot persistido** (`issuer_snapshot`/`customer_snapshot`, ADR-001) en los DOS caminos de detección (same-ID y fila reemplazada), nunca una fila viva; fallback honesto a filas vivas solo para proformas congeladas sin snapshot (limitación documentada). Antes de implementar se verificó que los DOS generadores del snapshot (modelo `Invoice` + `InvoiceService`) persisten las mismas claves con fallbacks simétricos, evitando diffs fantasma. Guard tests: «congelado sin ediciones → cero cambios» + camino legacy sin snapshot. Fix vía TDD (5 tests), PR #109.
  - **v6.3.0** (2026-07-12) — **AID-442 + AID-450** (una migración, limpieza de datos guardada): los 6 PDFs ganan la fila condicional «Fecha de operación» (art. 6.1.f RD 1619/2012 — solo cuando `service_date` ≠ `invoice_date` y no es null; la regla de visibilidad vive en `DomPDFService::prepareTemplateData()`, los blades quedan tontos, sin `if proforma` — la exclusión de proforma es capa de datos, AID-444); retirada la plantilla fantasma `proforma-simple` del seeder (el blade nunca existió — crash `View not found` latente) + migración de limpieza guardada por `View::exists()` que borra la fila ya sembrada en BDs existentes conservando overrides del consumidor + guardarraíl registro↔miembros (test que asserta que cada `template_path` sembrado resuelve a vista real). Lección de proceso: los gates de inventario (entrada del manifest `in_base: false`, conteo del install test) viajan en el PR de feature; el re-stamp release/base en el de release.
  - **v6.2.0** (2026-07-12) — **AID-391 + AID-439** (cero migraciones): los 6 PDFs imprimen `invoice_date` («Fecha de expedición», RD 1619/2012 art. 6.1.b) en vez de `created_at`, con test de regresión que renderiza las 6 plantillas con modelo real (cazó y corrigió el crash latente `ucfirst(status)` en 3 hermanas); `EuSalesThresholdService` falla loud vía la nueva **`MissingInvoiceOwnerException` (`@api` — la adición que hace minor esta release)** y deriva `fiscal_year` de `RegionalContext` (no `date('Y')`); DomPDF inyectable en `PDFService` + escrituras vía `File` facade (`@internal`); limpieza idiomática (`uniqid()` fuera, N+1 de `GroupedPaymentService` batcheado). El display de `service_date` quedó en AID-442/AID-444 (backlog).
  - **v6.1.0** (2026-07-11) — **AID-429, primer MINOR con migración bajo `STABILITY.md`**: `invoices.prefix` + `invoice_series_control.prefix` de varchar(10)→50 (límite derivado del XSD AEAT: `NumSerieFactura` maxLength 60 − 10 dígitos de correlativo); DDL data-safe (solo ensancha); regla 2 de `STABILITY.md` generalizada (ritual de upgrade para TODA release con migraciones, major o minor).
  - **v6.0.0** (2026-07-11) — **AID-423, BREAKING (major de CIERRE)**: retirada TODA la superficie `@deprecated` — `BillingService` (clase entera → `InvoiceService`), los shims `user_id` de `UserTaxProfile` (→ superficie `owner_user_id`/`owner()`), los aliases de `HasUserRelationships` y `UserTaxProfileFactory::forUser()`. **Cero migraciones** (sin cambio de esquema/datos). Publica **`STABILITY.md`** (contrato de estabilidad, en el dist) y `UPGRADE-6.0.md` (renombres 1:1). Incluye el fix del race de `pest --parallel` (AID-419). El backlog de deprecated queda VACÍO.
  - **v5.0.0** (2026-07-11) — **AID-307, BREAKING**: separada la **serie fiscal real** (`invoices.prefix`) del **tipo fiscal** (`InvoiceSerieType`); el unique de `invoices` pasa a `(prefix, serie, series_number, fiscal_year)`; `VerifactuAdapter` envía la serie real como `NumSerieFactura`; nuevo `InvoiceSeriesResolver` (fuente única, multi-serie configurable). Migración DDL-only (superset). Empaqueta también **AID-412** (contract tests + snapshots) y **AID-413** (taxonomía `@api`/`@internal`). Desbloquea AID-289 del consumidor `clientes`.
  - **v4.1.0** (2026-07-10) — **AID-390**: numeración fiscal endurecida y consolidada (ver bullet propio). Precedida por la auditoría de antipatrones del 2026-07-10 (2 Critical: `rand()` y contador en cache).
  - **v4.0.1/v4.0.2** (2026-07-04) — **AID-324/325**: `UPGRADE-4.0.md` empaquetado en el dist + preflight double-proof de lararoi v1.0.4 para la tabla legacy `vat_verifications`.
  - **v4.0.0** (2026-07-03) — **AID-309, BREAKING**: retirada la capa VAT/ROI propia; el dominio lo posee `lararoi ^1.0` vía puente fino (`Actions\VerifyVatNumber`).
  - **v3.x / v2.x / v1.0 (jun-2026, condensado):** v3.1.x = PHPStan L5→L8 (AID-277/279/281) + grouped payments (AID-30/272/273) + `AeatInvoiceValidator` muerto fuera (AID-280); v2/v3 cierran «nada decimal/float en BD» (AID-240/242/246); v1.0.0 = migración completa a `FixedDecimal` de lara100 v2 (AID-237 — query builder devuelve int, atributo Eloquent devuelve `FixedDecimal`).
- **Tipo fiscal ≠ serie fiscal (AID-307, v5.0.0).** `InvoiceSerieType` (`serie`) es el **TIPO** (→ AEAT `TipoFactura` F1/F2/R1); la **SERIE real** vive en `invoices.prefix` (→ AEAT `NumSerieFactura`). El unique de `invoices` es `(prefix, serie, series_number, fiscal_year)`. La serie se resuelve SIEMPRE por `InvoiceSeriesResolver` (`@api`, fuente única: request `invoiceData['series']` → config `invoice_numbering.series.{tipo}` → legacy prefix → `enum::defaultPrefix()`), nunca hardcode. Multi-serie por tipo configurable (RD 1619/2012 art. 6). `prefix` **no** se renombró (reconceptualizado — snapshots de contrato intactos). La cadena Verifactu es **global por obligado tributario, nunca por serie** (cambio de serie intra-ejercicio es transparente; lara-verifactu es single-OT por instalación).
- **Numeración fiscal: dueño único `InvoiceNumberingService` (AID-390, v4.1.0).** Scope de EMISOR con sentinel `InvoiceSeriesControl::GLOBAL_SCOPE` (nil-UUID; NULL retirado — no colisiona en índices unique de MySQL/SQLite), creación de primer uso idempotente (catch de unique race + relectura con lock; `DB::transaction(..., 3)` ante deadlocks de gap-lock), semilla de continuidad desde `MAX(series_number)` por **(prefix/serie, ejercicio)** (scopeada por prefix desde AID-307). `InvoiceService` y `RecurringBillingService` delegan — los generadores legacy (`rand()`, contador en cache, MAX+1) NO existen ya, y `BillingService` fue **retirado en v6.0.0** (AID-423). Test fork-based en `tests/Concurrency/InvoiceNumberingConcurrencyTest.php` (gate `RUN_CONCURRENCY_IT=1` + MySQL), con sensibilidad demostrada.
- **Gates de contrato (AID-412/413, v5.0.0):** golden-master de la superficie de los 7 modelos de contrato (`tests/Contract/snapshots/`, regen SOLO vía `bin/sync-contract-snapshots` con gate de CHANGELOG); manifest de inmutabilidad de migraciones shipped (`release-migration-manifest.json` + `ShippedMigrationImmutabilityTest`); test de upgrade-path MySQL (`tests/Integration/UpgradePath/`); taxonomía `@api`/`@internal` en las 123 clases de `src/` (`SurfaceTaxonomyTest`, criterios en `docs/api-surface.md`). Cualquier cambio de superficie/esquema es ruidoso y consciente — no más breaking silenciosos. `bin/tag-release` corre el preflight de contrato + manifest antes de taguear.
- **Política de upgrade (AID-398, «versión estable = respeto por datos»):** todo cambio de esquema/semántica de datos lleva su programa de actualización en el MISMO PR — regla dura en el CLAUDE.md del paraguas. El README documenta el camino real: `composer update` + re-run idempotente de `larabill:install` (publica solo migraciones nuevas por nombre; `vendor:publish` sin `--force` no machaca el config del consumidor) + `migrate`. NUNCA recomendar `migrate:fresh` sobre datos reales. Precedente v5.0.0: en una major con migración, la promoción de CHANGELOG + regen de manifest + `UPGRADE-X.md` viajan en el MISMO PR (no en un `chore(release)` aparte). Matiz v6.0.0: en una major SIN migraciones el manifest no cambia con el código — solo su stamp release/base, que regenera el preflight de `bin/tag-release`; ese stamp sí viaja en un `chore(release)` posterior al merge.
- **Próximo breaking conocido: NINGUNO.** El major de higiene se ejecutó como **v6.0.0** (AID-423) y vació el backlog de deprecated. Desde v6.0.0 rige el **contrato de estabilidad `STABILITY.md`** (en el dist): breaking solo con imperativo de uso cualificado y documentado; todo major auto-upgradeable desde el anterior (maquinaria AID-398); deprecación viva ≥1 major completo antes de retirarse. Un feature/fix que proponga romper superficie sin caso real documentado **contradice el contrato** — parar y confrontar con `STABILITY.md`. `FiscalNumberAllocator` (lado consumer, AID-289) quedó **cableado en `clientes`** (emisión Vía 1). **v7 fue CANCELADO (2026-07-18):** línea 6.x fijada; cualquier major exige el registro central de gobierno (`approvals/majors/`, vacío) — el hardening que valía la pena se extrajo como correcciones 6.x (v6.5.0). AID-444 quedó **Canceled** (su fondo lo resolvió AID-559/D10: `service_date` se arrastra verbatim, jamás se inventa; contexto fiscal interno en `docs/2026-07-12-proforma-service-date-fiscal-rationale.md`, gitignorado). **Backlog efectivo del paquete: CERO.** **AID-576** (serie per-call en la conversión) entregado en el paquete como v6.7.0; su lado consumer (reconciliación de la conversión manual Vía 1 de Castris, puerto+adapter) queda en el repo `clientes` y absorbe AID-416. **AID-581** (flake del factory `is_roi_taxed`) cerrado el mismo día. Ticket de paquete aún deferred por diseño: **AID-560** (D5, elegir serie fiscal destino en la conversión), Low («solo con consumidor real que lo pida») — nota: el mecanismo que necesitaría ya existe (la opción `series` de v6.7.0). La suite de conformance del consumidor de referencia (AID-501, Castris) está entregada y Done.
- **Deuda "nada decimal en BD": CERRADA** (AID-240/242/246 — AID-286 documenta el cierre). Cero columnas `decimal`/`float` en migraciones; todos los modelos monetarios/tasa usan `FixedDecimalCast:2`. Único superviviente con cast `integer` **por decisión deliberada**: `tax_rates.rate` (núcleo fiscal vía `VatCalculationStrategy`, base-100 del %, `/10000` → fracción; mayor riesgo, fuera de scope salvo caso concreto).
- **Contrato de retención legal (AID-221):** `Invoice` y `UserTaxProfile` implementan `AichaDigital\LaraPrivacyCore\Contracts\LegallyRetainable` (único método `retainedUntil(): ?DateTimeInterface`). El contrato lo **posee el core** (`lara-privacy-core`); larabill es un adapter. El gate `isUnderRetention` lo resuelve `CheckLegalHold` del core — **sin** `RetentionBasis`/`legalHold()`/`isUnderRetention()` en los modelos. `Invoice`: fiscal → fin de ejercicio de `invoice_date` + `larabill.retention.fiscal_years` (env `LARABILL_RETENTION_FISCAL_YEARS`, default 6); PROFORMA/sin fecha → null. `UserTaxProfile`: MAX sobre sus facturas (computa aun soft-deleted; huérfano → null). Calcular, no materializar.
- **Dependencias notables:** `lara-verifactu ^1.0` (estable), `lara-privacy-core ^1.0`, `lara100 ^2.0` (FixedDecimal — AID-237), `lararoi ^1.0.4` (puente fino de verificación VAT/NIF — AID-309; el `.4` garantiza el preflight double-proof de AID-325), `dompdf/dompdf ^3.1`.
- Bot de dependencias: **Renovate self-hosted** (`renovate.tabratino.com`). Ver paraguas para el protocolo de transición.
- **Contrato vigente:** UUID-first total (ADR-006). `users.id` debe ser UUID v7 char(36); el `larabill:install` aborta con mensaje accionable si no lo es. La superficie agnóstica (`int`/`ulid`, `larabill.user_id_type`, `LARABILL_USER_ID_TYPE`, flag CLI `--user-id-type=`) fue retirada en v0.8.0.
- **Migraciones (ADR-007 + ADR-010):** `.php` = fuente de verdad, `.php.stub` = artefacto derivado byte-exacto (`bin/sync-migration-stubs`); `$migrationOrder` 1:1 con los stubs, validado por `MigrationOrderConsistencyTest`. Constantes de fixture: `tests/TestCase::USER_UUID_1/2/3`.
- **Publishing de stubs: se MANTIENE (AID-302 → ADR-010, 2026-07-02).** El refactor a «0 stubs» (esquema package-managed, como laratickets AID-290) queda **RECHAZADO/aplazado**: sería breaking para consumidores ya instalados sin beneficio correctivo (la deuda técnica ya estaba cerrada). Reconsiderar solo en una versión mayor breaking con plan de migración explícito — criterios de reapertura en `docs/ADR-010-keep-migration-stub-publishing.md`. AID-302 describía el estado v0.8.3 (6 divergencias) ya superado por ADR-007.
- **Deuda de divergencia `.php`↔`.stub`: ENTERAMENTE SALDADA.** Los 3 follow-ups de ADR-007 cerrados: (1) test de instalación productiva en MySQL (`tests/Integration/InstallMysql/InstallCommandSchemaTest.php`, AID-287) valida `larabill:install → stubs → migrate` con FKs íntegras; (2) auditoría del resto del stack (barrido umbrella AID-298…303); (3) el histórico fillable huérfano `tax_group_id` en `InvoiceItem` verificado **inexistente** en v3.1.3 — no se persiste en el item por diseño (snapshot inmutable en `taxes_applied`/`total_tax_amount`; `tax_group_id` es columna solo en `articles` + input de `TaxCalculationService`).
- **Tests:** SQLite (`composer test`) + MySQL Integration (`tests/Integration/Mysql/`). CI matriz PHP 8.3+8.4 × L12+L13 + job MySQL 8 + **mutation testing** (`pest --mutate`, job no-required, gate `--min=68` pinneado del baseline medido, scope `mutates()` en las suites de money+fiscal — AID-536). PHP 8.4 local: bug "table already exists" → usar PHP 8.3 o confiar en CI.
- **Onboarding consumidor:** `docs/setup-uuid.md` es la guía canónica.
